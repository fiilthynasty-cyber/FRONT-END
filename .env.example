# Optional: leave empty to use Vite proxy mode for /api/*
VITE_API_BASE_URL=

# Backend route used by dashboard
VITE_DASHBOARD_PATH=/api/dashboard

# Dev proxy target (used by Vite dev server)
VITE_PROXY_TARGET=http://localhost:5000

# Request timeout in milliseconds
VITE_REQUEST_TIMEOUT_MS=8000
